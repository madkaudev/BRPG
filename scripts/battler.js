class Battler {
    // Declare constructor
    constructor(heroes, enemies) {
        this.heroes = heroes;
        this.enemies = enemies;
        this.characters = heroes.concat(enemies);
    }

    // Function to order the characters based on speed and break ties
    orderCharacters() {

    }

    // Function to process physical attack
    battle() {

    }
}